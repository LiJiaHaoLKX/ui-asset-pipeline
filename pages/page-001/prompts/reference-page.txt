Create a production-ready screenshot of a real desktop application page named “测试”, not a poster, device mockup, browser frame, or marketing graphic. Render at exactly 1440 × 900 px with device pixel ratio 1. Use a strict 4 px spacing system, 32 px page padding on all sides, and 24 px gaps between sections. Use Inter throughout, with the visible page title exactly “测试” at 32 px and body text at 14 px with 1.5 line height. Background color: #0B1117. Surface color: #12232D. Primary accent color: #20E5B2. Text color: #F4F7F8. Cards use an 8 px corner radius; buttons use a 6 px corner radius. Build a restrained, polished application interface with the title at the top of the content area and simple empty test-page surfaces below it, aligned to the spacing grid. Do not invent additional visible copy, branding, navigation labels, metrics, illustrations, gradients, or decorative imagery. Keep all structural UI crisp, flat, and feasible to recreate exactly in HTML/CSS; no complex raster artwork is required. Maintain clear contrast, precise alignment, generous negative space, and keep every element inside the 32 px safe area.
